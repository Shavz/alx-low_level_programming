#include "main.h"
/**
 * mul - a function that multiplies two integers
 * @a: first variable
 * @b: second variable
 * multiply both variable inputs
 * Return: both variables multiplied
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
